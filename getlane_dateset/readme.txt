操作流程:
1:将视频路径改为本地路径, 创建处理视频对应的标签文件夹, 视频路径设置在main(), 标签文件路径设置在getparameter()函数;
2:运行程序,沿着要标记的车道线左侧(或右侧)轮廓,画直线, 标记完所有车道线后, 任意键开始识别下一帧;
3:任意键开始识别下一帧, 若此帧检测错误, 则按小写r键, 重新标记车道线 (之前检测结果仍会显示), 任意键开始识别下一帧;
4:小写q键, 程序退出.

注意: 本程序基于opencv3;
      直线存储形式为一般式:AX+BY+C=0;

版本2:新增:车道线偏移限制, 设置车道线检测起点
